{
    "name": "filter_string_length",
    "consumes": ["list(string)"],
    "produces": ["list(string)"],
    "init_args":[
        {
            "name": "word_length",
            "type": "integer",
            "required": true
        },
        {
            "name": "comparison",
            "type": "string",
            "required": true
        }
    ],
    "description": "This function takes a list of strings and two parameters (word_length and comparison)."
}
